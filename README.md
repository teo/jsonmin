### jsonmin ###

A JSON minifier written in Go, based on [JSON.minify](https://github.com/getify/JSON.minify).

jsonmin minifies blocks of JSON-like content into valid JSON by removing all whitespace *and* comments.

##### usage #####
Import "github.com/teo/jsonmin" and call jsonmin.Minify on your document.
